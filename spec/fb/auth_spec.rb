require "spec_helper"

RSpec.describe Fb::Auth do
  it "has a version number" do
    expect(Fb::Auth::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
